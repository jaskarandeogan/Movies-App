import { View, Text } from 'react-native'
import React from 'react'

const IndividualCard = (navigation) => {
    const [movie, setMovie] = React.useState({})

    // React.useEffect(() => {

    // }
  return (
    <View>
      <Text>IndividualCard</Text>
    </View>
  )
}

export default IndividualCard